#include <bits/stdc++.h>
using namespace std;
int main()
{
    int testCases, charecterLength;
    string meowSound;
    cin >> testCases;
    for (int i = 0; i < testCases; i++)
    {
        cin >> charecterLength;
        for (int j = 0; j < charecterLength; j++)
        {
            cin >> meowSound;
            char firstMewo = meowSound(j);
            if(firstMewo == 'm' || 'M'){

            }
        }
        
    }
    
return 0;
}